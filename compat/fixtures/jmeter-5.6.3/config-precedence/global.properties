# Global properties selected with -G<file>. These enter the remote property
# map, not the local JMeter or Java system property maps.
cfg.global.file=global-file
cfg.global.duplicate=global-first
cfg.global.duplicate=global-last
cfg.remote=global-file-remote
