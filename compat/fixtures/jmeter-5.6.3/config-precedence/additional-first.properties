# First additional JMeter property file selected with -q.
cfg.source=additional-first
cfg.q.repeat=additional-first
cfg.repeat=additional-first
cfg.escaped=additional\=first
cfg.empty=
cfg.duplicate=additional-first
